<?php
/**
 * @var \Zend\View\Renderer\PhpRenderer $this
 * @var array $values
 * @todo Remove this partial.
 */

$item_property = [];

foreach ($values as $propertyData) {
    foreach ($propertyData['values'] as $value) {
        $item_property[$propertyData['property']->term()][] = trim($value->value());
    }
}

echo json_encode($item_property);
